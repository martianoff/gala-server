load("@gala//:gala.bzl", "gala_binary")

gala_binary(
    name = "hello",
    src = "main.gala",
    gala_deps = [
        "//:gala-server",
    ],
    deps = [
        "//httpcore",
        "@gala//collection_immutable",
        "@gala//concurrent",
        "@gala//json",
    ],
)
