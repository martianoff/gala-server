load("@gala//:gala.bzl", "gala_binary")

gala_binary(
    name = "hello",
    src = "main.gala",
    gala_deps = [
        "//:gala-server",
    ],
    deps = [
        "@gala//collection_immutable",
    ],
)
